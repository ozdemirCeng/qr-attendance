# Vercel Web project template (Root Directory: apps/web)
# Add these values in Vercel -> Project Settings -> Environment Variables.

NODE_ENV=production

# API domain used by web app requests
NEXT_PUBLIC_API_URL=https://qr-attendance-api-eta.vercel.app

# Recommended for same-origin proxy route handlers
API_INTERNAL_URL=https://qr-attendance-api-eta.vercel.app
