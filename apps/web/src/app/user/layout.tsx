export const metadata = {
  title: "Kullanıcı Alanı",
  description: "Katılımcı paneli",
};

export default function UserLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
